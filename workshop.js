// Player image (the Ohio State logo)
var playerLogoUrl = "./assets/OhioStateLogo.jpg";

// Player size
var playerSize = 80;

// Player speed
var playerSpeed = 2;

// Obstacle image (the Michigan logos)
var obstacleLogoUrl = "./assets/MichiganLogo.png";

// Obstacle size
var obstacleSize = 80;

// Message when you win
var winningText = "TOUCHDOWN!";

// Color of message when you win
var winningColor = "red";

// Message when you lose
var losingText = "You got tackled!";

// Color of message when you lose
var losingColor = "blue";

// Goal image
var goalImageUrl = "./assets/EndZone.png";

// Background image (WIP)
var backgroundImageUrl = './assets/FootballField.jpg';